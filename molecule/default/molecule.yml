---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint:
  name: yamllint
platforms:
  - name: instance
    box: ${VAGRANT_DEFAULT_BOX:-generic/rhel7}
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  inventory:
    host_vars:
      instance:
        rhsub_username: ${RHSM_USERNAME}
        rhsub_password: ${RHSM_PASSWORD}
verifier:
  name: testinfra
  lint:
    name: flake8
